# ============================================================
# PaySentinelIQ — Workers (standalone processes)
# ============================================================
# Run with:  python -m app.workers.<name>
# Each worker is horizontally scalable: run N instances and
# RabbitMQ fair-dispatches messages across them (prefetch).
# ============================================================
